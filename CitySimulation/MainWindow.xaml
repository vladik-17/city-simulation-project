<Window x:Class="CitySimulation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:CitySimulation.Views"
        Title="City Simulation" 
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Вкладки как на картинке - горизонтальное меню -->
        <Border Grid.Row="0" Background="White" Padding="0" BorderThickness="0,0,0,1" BorderBrush="LightGray">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="ResourcesTab" Content="Добыча ресурсов (17)" 
                        Click="ResourcesTab_Click"
                        Background="Transparent"
                        Foreground="Black"
                        FontSize="12"
                        FontWeight="Normal"
                        Padding="15,8"
                        BorderThickness="0"
                        Cursor="Hand"/>
                <Button x:Name="UtilitiesTab" Content="Коммунальные услуги (15)" 
                        Click="UtilitiesTab_Click"
                        Background="Transparent"
                        Foreground="Black"
                        FontSize="12"
                        FontWeight="Normal"
                        Padding="15,8"
                        BorderThickness="0"
                        Cursor="Hand"/>
            </StackPanel>
        </Border>

        <!-- Контент вкладок -->
        <Grid Grid.Row="1">
            <!-- Вкладка добычи ресурсов -->
            <ContentControl x:Name="ResourcesContent" Visibility="Visible">
                <views:ResourcesView/>
            </ContentControl>

            <!-- Вкладка коммунальных услуг -->
            <ContentControl x:Name="UtilitiesContent" Visibility="Collapsed">
                <views:UtilitiesView/>
            </ContentControl>
        </Grid>
    </Grid>
</Window>