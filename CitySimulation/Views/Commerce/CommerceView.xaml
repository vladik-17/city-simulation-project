<UserControl x:Class="CitySimulation.Views.Commerce.CommerceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Коммерческая деятельность" 
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListView Grid.Row="1" ItemsSource="{Binding CommercialBuildings}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название" DisplayMemberBinding="{Binding Name}" Width="120"/>
                    <GridViewColumn Header="Тип" DisplayMemberBinding="{Binding CommercialType}" Width="100"/>
                    <GridViewColumn Header="Доход" DisplayMemberBinding="{Binding DailyIncome}" Width="80"/>
                    <GridViewColumn Header="Сотрудники" DisplayMemberBinding="{Binding EmployeeCount}" Width="80"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Border Grid.Row="2" Background="LightBlue" Padding="10" Margin="0,10,0,0">
            <TextBlock Text="{Binding TotalDailyRevenue, StringFormat='Общий ежедневный доход: {0:C}'}" 
                       FontWeight="Bold"/>
        </Border>
    </Grid>
</UserControl>